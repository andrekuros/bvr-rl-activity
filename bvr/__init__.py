"""Simplified Beyond Visual Range (BVR) air-combat RL teaching package.

Students only edit `config/rewards.json` (reward/penalty values, zero allowed)
and `config/scenario.json` (enemy mix). The neural-network architecture
(`bvr/policy.py`) and the PPO hyperparameters (`bvr/train.py`) are LOCKED so the
online competition is fair.
"""

__version__ = "0.1.0"
